<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Weather App</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container d-flex flex-column justify-content-center align-items-center" style="height: 100vh;">
    <div class="card">
      <div class="card-body">
        <form action="/weather" method="get">
          <input type="text" class="form-control form-control-lg" name="city" placeholder="Enter City Name, ex: London" style="min-width: 400px;" value="<%= city %>">
          <button type="submit" class="mt-3 btn btn-success btn-lg">Get Weather</button>
        </form>
      </div>
    </div>
    <% if (data) { %>
      <div class="card mt-4">
        <div class="card-header bg-primary text-white">Weater for: <%= city %></div>
        <div class="card-body">
          <div style="min-width: 400px;">
            <p>Current Temp : <%= data.main.temp %> <%= symbol %>, <%= data.weather[0].description %></p>
            <p>Humidity : <%= data.main.humidity %> %</p>
            <p>Min : <%= data.main.temp_min %> <%= symbol %></p>
            <p>Max : <%= data.main.temp_max %> <%= symbol %></p>
          </div>
        </div>
      </div>
    <% } %>
  </div>
</body>
</html>